# Agent Guidelines

- Never add binary or non-text assets (e.g., PNG, JPG, executables) to this repository. Use text-based or vector alternatives instead.
- Favor editable text-based formats (TypeScript, JSON, Markdown, SVG, etc.) for any new assets.
